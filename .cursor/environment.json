{
  "$schema": "https://www.cursor.com/schemas/environment.schema.json",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "install": "python -m pip install --upgrade pip && python -m pip install -e '.[test]'"
}
