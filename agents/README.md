# Agents ontology

Stage scripts and parameter profiles moved to [`anchors/`](../anchors/README.md).

This directory now holds **machine-readable ontology** only:

| Path | Purpose |
|---|---|
| [`ontology/`](ontology/) | SAM4Tun segment schema, tunnel priors, YAML ontology |

For pipeline profiles, frozen runs, and run commands see:

- [`anchors/README.md`](../anchors/README.md) — parameter snapshots per family
- [`data/anchors/README.md`](../data/anchors/README.md) — frozen artifact trees
