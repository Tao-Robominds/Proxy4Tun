# Experiment outputs

`data/subsets/` holds the labelled input clouds. Write every new run under
`data/<experiment-id>/`. Do not write into `releases/1.0/`.

Release 1.0 results (anchors, BO, refinement, SC-general) live in
`releases/1.0/data/`.
